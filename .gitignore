# Python
__pycache__/
*.pyc
*.pyo
venv/

# YOLO model weights (download on first run, ~6MB each)
*.pt

# SQLite detection buffer
*.db

# Node
node_modules/
dist/

# Env files
.env

# macOS
.DS_Store

# Xcode user state
ios/SeaToSkyCounter.xcodeproj/xcuserdata/
ios/SeaToSkyCounter.xcodeproj/project.xcworkspace/xcuserdata/

# Captured frames from Pi (training data — large, managed separately)
pi/captures/

# ML dataset bulk — track only data.yaml, ignore images/labels
ml/data/labeled/*
!ml/data/labeled/data.yaml
ml/data/test_holdout/
ml/data/unsorted/
ml/runs/

# CoreML model package (binary weights — not suitable for git)
ios/yolov8n.mlpackage/
